name: Build and Push Frontend Image

on:
  # Manual execution
  workflow_dispatch:
  # Automatic on direct push to develop
  push:
    branches:
      - develop
  # Automatic when a PR to develop or main is closed/merged
  pull_request:
    types: [closed]
    branches:
      - develop
      - main

jobs:
  build-and-push:
    name: Build and Push to Docker Hub
    runs-on: ubuntu-latest
    # Run on push to develop OR on a merged pull request to develop/main
    if: |
      github.event_name == 'push' || 
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)

    permissions:
      contents: read
      actions: write

    env:
      DOCKERHUB_REPO: carlosmca/envialo-web-app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # If it's a pull_request, checkout the merge commit
          ref: ${{ github.event.pull_request.merge_commit_sha || github.sha }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Determine tag suffix
        id: tag-suffix
        run: |
          # If it's a pull_request merged to main, use prod
          # If it's a push to develop, use dev
          if [ "${{ github.event_name }}" = "pull_request" ] && [ "${{ github.event.pull_request.merged }}" = "true" ]; then
            echo "suffix=prod" >> $GITHUB_OUTPUT
          else
            echo "suffix=dev" >> $GITHUB_OUTPUT
          fi

      - name: Get commit SHA
        id: sha
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "sha=${{ github.event.pull_request.merge_commit_sha }}" >> $GITHUB_OUTPUT
          else
            echo "sha=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKERHUB_REPO }}
          tags: |
            type=raw,value=${{ steps.tag-suffix.outputs.suffix }}
            type=raw,value=${{ steps.tag-suffix.outputs.suffix }}-${{ steps.sha.outputs.sha }}

      - name: Set build args with environment-specific values
        id: build-args
        run: |
          ENV_SUFFIX="${{ steps.tag-suffix.outputs.suffix }}"
          
          if [ "$ENV_SUFFIX" = "prod" ]; then
            # PRODUCTION
            API_BASE_URL="${API_BASE_URL_PROD:-${API_BASE_URL:-https://api.envialo.brinxpress.com}}"
            SESSION_SECRET="${SESSION_SECRET_PROD:-${SESSION_SECRET:-}}"
            NODE_ENV="production"
          else
            # DEVELOPMENT
            API_BASE_URL="${API_BASE_URL_DEV:-${API_BASE_URL:-https://api.envialo.brinxpress.com}}"
            SESSION_SECRET="${SESSION_SECRET_DEV:-${SESSION_SECRET:-}}"
            NODE_ENV="development"
          fi
          
          # COMMON
          NODE_TLS_REJECT_UNAUTHORIZED="${NODE_TLS_REJECT_UNAUTHORIZED:-0}"
          
          # Export to GITHUB_ENV
          echo "API_BASE_URL=$API_BASE_URL" >> $GITHUB_ENV
          echo "SESSION_SECRET=$SESSION_SECRET" >> $GITHUB_ENV
          echo "NODE_ENV=$NODE_ENV" >> $GITHUB_ENV
          echo "NODE_TLS_REJECT_UNAUTHORIZED=$NODE_TLS_REJECT_UNAUTHORIZED" >> $GITHUB_ENV
          
          echo "=== Variables to be used for $ENV_SUFFIX ==="
          echo "API_BASE_URL=$API_BASE_URL"
        env:
          # GitHub Vars (not secrets) - Dev
          API_BASE_URL_DEV: ${{ vars.API_BASE_URL_DEV }}
          SESSION_SECRET_DEV: ${{ secrets.SESSION_SECRET_DEV }}
          # GitHub Vars (not secrets) - Prod
          API_BASE_URL_PROD: ${{ vars.API_BASE_URL_PROD }}
          SESSION_SECRET_PROD: ${{ secrets.SESSION_SECRET_PROD }}
          # Fallbacks
          API_BASE_URL: ${{ vars.API_BASE_URL }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          NODE_TLS_REJECT_UNAUTHORIZED: ${{ vars.NODE_TLS_REJECT_UNAUTHORIZED }}

      - name: Build and push Frontend Application image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          no-cache: true
          build-args: |
            API_BASE_URL=${{ env.API_BASE_URL }}
            SESSION_SECRET=${{ env.SESSION_SECRET }}
            NODE_ENV=${{ env.NODE_ENV }}
            NODE_TLS_REJECT_UNAUTHORIZED=${{ env.NODE_TLS_REJECT_UNAUTHORIZED }}
